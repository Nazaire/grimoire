# Lifecycle

The process. `Lifecycle` is a one-shot start/stop shell. `App` is the tree —
shared infra, then HTTP and/or workers by **role**. A domain lists its workers;
infra `Workers` composes those groups.

## Topics

| Topic        | Status    |
| ------------ | --------- |
| [app](./app) | ✅ Active |
